/** Shared outline action buttons — distinct from filled nav tabs (`bg-accent`). */

const actionBase =
  "rounded border border-accent px-3 py-1.5 text-xs font-normal transition-colors disabled:opacity-40";

export const actionButtonClass = `${actionBase} text-gray-200 hover:bg-accent/10`;
